* DECLARAÇÃO DO CIRCUITO

.subckt escrita bit we bl blb vdd gnd

* INVERSOR DO BIT
M1_bit vdd bit bitb vdd PMOS w =w_esc_pmos l =length
M2_bit bitb bit gnd gnd NMOS w =w_esc_nmos l =length

* INVERSOR DO WRITE ENABLE (WE)
M1_we vdd we web vdd PMOS w =w_esc_pmos l =length
M2_we web we gnd gnd NMOS w =ww_esc_nmos l =length

* ESCRITA EM BL
M1 gnd bitb out_bl gnd NMOS w =w_esc_nmos l =length
M2 out_bl we bl gnd NMOS w = w_esc_nmos l =length
M3 vdd bitb out_bl vdd PMOS w =w_esc_pmos l =length
M4 out_bl web bl vdd PMOS w =w_esc_pmos l =length

* ESCRITA EM BLB
M5 gnd bit out_blb gnd NMOS w =w_esc_nmos l =length
M6 out_blb we blb gnd NMOS w =w_esc_nmos l =length
M7 vdd bit out_blb vdd PMOS w =w_esc_pmos l =length
M8 out_blb web blb vdd PMOS w =w_esc_pmos l =length

.end subckt

.end
